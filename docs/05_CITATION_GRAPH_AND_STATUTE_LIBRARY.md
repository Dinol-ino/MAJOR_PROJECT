# Module 5 — Citation Graph & Statute Library: Purpose, Cold-Start State, Dynamic Growth

**Scope**: `app/routes/statutes.py` (`/statutes/catalog`, `/statutes/{act_id}/tree`, `/statutes/graph`, `/statutes/acts`, `/statutes/acts/{act_id}`), `CitationGraphView.jsx`, `StatuteLibraryView.jsx`.

**Evidence context**: Img 1 shows a Citation Graph with a "Section 66" node inspector displaying real statutory text, connected relationships ("Requires Violation of," "Penalizes With," "Judicially Construed In" — pointing to *Shreya Singhal v. UOI (2015)*), and quick-add seed pills (`+ IT Act Sec 66`, `+ Companies Act Sec 166`, `+ Shreya Singhal`, `+ Contract Sec 73`). Img 8 (prior session) shows a sparser view of the same graph with a "Section 1" node. Img 2 shows a full Statute Library reader for IT Act §66 with clean official-text rendering, penalty box, and "Research with AI Copilot" button. **This is good work** — the node inspector, the relationship typing (Contains/Penalizes With/Judicially Construed In/Cross-Applies Criminal Intent), and the statute reader are exactly the right shape. The problem your own audit flagged isn't the UI pattern, it's that the graph is "~10-node seed" rather than something that grows from real corpus + real usage.

---

## 5.1 What "seed pills" should and shouldn't mean

The `+ IT Act Sec 66`, `+ Shreya Singhal` pills in Img 1 are a fine UX pattern *if* they're shortcuts into a real, larger graph — the equivalent of quick-search suggestions. They become the audit-flagged problem only if the entire graph *is* those four seeds plus their immediate neighbors and nothing else exists behind them.

**Task 5.1.1**: Verify via direct query (bypass the UI) how many total nodes and edges exist in whatever backs `GET /statutes/graph` today. Compare against the corpus coverage number from Module 1 Task 1.3.1. The graph's node count should scale with real corpus size (every ingested section is a potential node; every actual citation, cross-reference, or "Penalizes With" relationship extracted from statutory text is a potential edge) — it should not be a fixed set independent of what's been ingested.

## 5.2 `GET /statutes/graph` — the graph must be derived, not authored

**Graph Storage Backend — Memgraph Community Edition**:
Replace the current graph storage assumption (implicit Postgres/JSON-backed traversal) with **Memgraph Community Edition** as the backend for `GET /statutes/graph`.
- *Reasoning*: The citation graph is a genuine multi-hop graph-traversal problem (Section → Penalty → Precedent → cross-referenced Section chains), which is poorly served by relational joins or in-memory JSON reconstruction on every request. Memgraph provides real Cypher querying, in-memory performance, and native support for typed, directed edges — which maps directly onto the `derivation_method` tagging scheme defined below (`corpus_structure`, `text_extraction`, `curated_legal_relationship`, `mcp_case_law_lookup`, `llm_suggested_unverified`). Store `derivation_method` and `cited_in_conversations` as edge/node properties natively in Memgraph rather than maintaining them in a separate relational side table.
- *Non-goal (Graphiti/Zep)*: Do NOT integrate Graphiti into Module 5. Graphiti is unsuitable for the citation graph because it is designed for temporal, LLM-extracted conversational memory (continuously evolving episodic facts), not the mostly-stable, corpus-derived structure this module needs — using it here would add unnecessary LLM-extraction latency and cost to a task that should be deterministic. (Flag Graphiti as a possible future candidate for Module 2's L3/L5 memory layers in `02_PROJECT_VAULT_AND_MEMORY.md` instead; explicitly out of scope for this pass).

**Task 5.2.1**: Define exactly where each edge type comes from, since "hardcoded vs dynamic" is really a question of provenance per edge:
- `Contains` (Act → Section): mechanical, derived directly from corpus structure at ingestion time. Always real once ingestion is real (Module 1 §1.3).
- `Penalizes With` (Section → Penalty): extracted at ingestion time from the section's own text (e.g. §66's penalty clause) via a deterministic parser, not invented. If extraction fails for a given section, no edge is created — do not fabricate a penalty relationship to keep the graph looking populated.
- `Requires Violation of`, `Cross-Applies Criminal Intent` (Section → Section): these are legal-interpretive relationships, not mechanical ones. These should NOT be silently generated by the LLM as ground truth — they need either (a) a curated relationship file maintained by a human legal reviewer per Act, versioned like the statutory corpus itself, or (b) if LLM-suggested, marked with a distinct visual treatment ("AI-suggested relationship — unverified") until reviewed. Presenting an LLM's inferred legal relationship with the same visual confidence as a mechanically-extracted one (as Img 1 currently does — all edges look identical) is itself a quiet hallucination-laundering path.
- `Judicially Construed In` (Section → Precedent): must trace to an actual case citation retrieved from a real source (the `indian-legal-gateway` MCP server, once Module 7 confirms it's live) — never invented case names. *Shreya Singhal v. Union of India (2015)* is a real, well-known landmark case about IT Act §66A, so this specific example is plausible, but the mechanism producing it needs the same provenance discipline as everything else, not "the LLM happened to know a real case this time."
- `Sanctions With`, `Governs Indemnity In`, `Validates Corporate Governance` (seen in the Companies Act portion of the earlier graph screenshot): same rule as above — mechanical relationships (defined by statute text) vs. interpretive ones (a legal judgment) must be visually distinguished.

**Task 5.2.2**: Add a `derivation_method` field to every graph edge (`corpus_structure`, `text_extraction`, `curated_legal_relationship`, `mcp_case_law_lookup`, `llm_suggested_unverified`) and render it in the node inspector's existing "Connected Statutory Relationships" panel (Img 1) as a small provenance tag next to each relationship — this reuses the same "Show Provenance Details" pattern already built for chat citations (Img 7), applied consistently to the graph.

**Task 5.2.3 (Hardware Tiering & Fallback)**: Memgraph runs as an additional service and has a non-trivial RAM footprint — cross-check against Module 3/6's hardware tiering. Recommend Memgraph only for Tier 1+ hardware (8GB+ RAM); on Tier 0 hardware, keep the existing lightweight in-process graph representation as a fallback, selected automatically based on the same hardware probe used for model tiering (`GET /system/hardware`).

## 5.3 Graph growth from chat activity (interconnection with Module 4)

**Task 5.3.1**: Per Module 4 §4.1's `ChatResponseFinalized` event: when a chat response cites a section not yet in the graph, upsert it with `derivation_method = corpus_structure` (it exists because it's in the corpus) and increment a `cited_in_conversations` counter used purely for graph node sizing/highlighting (frequently-cited sections render larger — a genuinely useful "what's actually being asked about" signal, unlike a static seed).

**Task 5.3.2**: User-uploaded documents (Tier 2, session-scoped) that reference statutory sections should also produce graph edges, tagged `derivation_method = user_document_reference`, scoped visually (e.g. dashed edge) to distinguish "this connection exists because of a specific client's uploaded contract" from "this connection is general Indian law." This is important for a legal tool — conflating a user's private matter with the general statutory graph without visual distinction risks confusing case-specific inference with settled law.

## 5.4 Statute Library — cold start and coverage honesty

**Task 5.4.1**: Img 2's library view (IT Act, Companies Act 2013, BNS 2023, Contract Act 1872, each with a real section count — "94 Sections," "470 Sections," "358 Sections," "238 Sections") needs those counts verified against actual ingested content, not the Act's real-world total section count copied from Wikipedia/general knowledge. If only some sections of the Companies Act are actually chunked and retrievable, the library should say "142 of 470 sections indexed" — the gap between "the Act has 470 sections" (a fact) and "we have 470 sections retrievable" (a claim about your system) is exactly the kind of thing that made the report.md "100% verified" framing misleading.

**Task 5.4.2**: "Research with AI Copilot" button (Img 2) should deep-link into `/chat` pre-seeded with the specific section as context — verify this actually passes the section's real chunk ID into the chat context rather than just pre-filling the text box with a section number and letting retrieval re-find it (small distinction, but the former guarantees the exact provision the user was reading is what gets analyzed; the latter risks retrieval pulling a different, similarly-worded chunk).

## Acceptance criteria for Module 5
- [ ] Graph backend is Memgraph on Tier 1+ hardware, verified via a real Cypher query against `/statutes/graph`; Tier 0 fallback verified separately.
- [ ] Every graph edge has a `derivation_method`, and interpretive/unverified edges are visually distinct from mechanical ones.
- [ ] Graph node/edge count is shown to scale with real corpus ingestion (test: ingest one new Act, confirm new Contains edges appear without a code deploy).
- [ ] A chat citation for a previously-absent section provably creates a new graph node within one request cycle.
- [ ] Statute Library section counts reflect actually-indexed sections, not the Act's nominal total.
